# Choose one provider: gemini or openai. If omitted, Life OS tries Gemini first, then OpenAI, then mock fallback.
AI_PROVIDER=gemini

# Gemini free-tier friendly setup.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-flash-lite

# Optional OpenAI setup.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-nano
