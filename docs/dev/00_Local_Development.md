---
status: Draft
version: 0.1
owner: LIN MENGLUNG
last_updated: 2026/07/07
depends:
  - docs/adr/ADR-0006-mvp-tech-stack.md
  - docs/architecture/00_MVP_Architecture.md
referenced_by: []
---

# 00 Local Development

## Purpose

This document explains how to run the Life OS MVP skeleton locally.

The current app is a scaffold only.

It does not implement the full MVP core loop.

For complete machine setup, read `docs/dev/01_Environment_Setup.md`.

For command reference, read `docs/dev/02_Dev_Commands.md`.

For host desktop runtime verification, read `docs/dev/03_Tauri_Runtime_Verification.md`.

For installed dependency records, read `docs/dev/04_Dependency_Installation_Log.md`.

## Prerequisites

- Node.js
- pnpm
- Rust toolchain
- Tauri system dependencies for your OS

## Install Dependencies

```powershell
pnpm install
```

If you prefer npm, the scripts are compatible with npm as well, but the current lockfile is generated by pnpm.

## Run Web Dev Server

```powershell
pnpm run dev
```

This starts the Vite dev server.

## Run Tauri Desktop App

```powershell
pnpm run tauri:dev
```

This starts the Tauri desktop shell.

## Current Placeholders

- SQLite dependency is present, but no schema or migrations exist.
- AI provider interface exists, but no real provider calls exist.
- Domain types are minimal and may change during MVP architecture work.
- The homepage is static and does not save user input.
